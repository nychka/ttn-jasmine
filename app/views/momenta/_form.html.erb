<%= form_for(@momentum) do |f| %>
  <% if @momentum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@momentum.errors.count, "error") %> prohibited this momentum from being saved:</h2>

      <ul>
      <% @momentum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
